module(
    name = "artificer",
    version = "0.1.0",
)

# Industry standard: Start simple, add dependencies only when needed
bazel_dep(name = "rules_python", version = "0.40.0")  # For Python REST API
bazel_dep(name = "rules_cc", version = "0.1.1")       # C++ build rules

# Python toolchain for REST API components
python = use_extension("@rules_python//python/extensions:python.bzl", "python")
python.toolchain(python_version = "3.12")